# regression_health core packages.
# Lower bounds are stated deliberately rather than exact pins. The pipeline relies
# only on long-stable APIs in these libraries, so any release at or above the floor
# reproduces the results, and a hard pin would force needless upgrades on a fresh
# machine. Pin exactly here if byte-identical reproduction is ever required.
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
pyyaml>=6.0        # reads config/config.yml (paths, seed, run parameters)
# optional but recommended
pyarrow>=12.0      # fast parquet I/O (panel currently saved as csv.gz fallback)
jupyter>=1.0
