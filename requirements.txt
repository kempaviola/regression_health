# regression_health — core packages (per ToU "Installing Packages Safely")
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
seaborn>=0.12
# optional but recommended
pyarrow>=12.0      # fast parquet I/O (panel currently saved as csv.gz fallback)
jupyter>=1.0
